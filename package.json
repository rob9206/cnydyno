{
  "name": "thunderhorse-tuning-site",
  "version": "1.0.0",
  "private": true,
  "description": "Thunderhorse Tuning — pre-rendered static site + Netlify Functions for the live job status portal.",
  "dependencies": {
    "@netlify/blobs": "^10.0.10"
  }
}
